<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Kyvex API</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <%- include('../partials/header', { username: username }) %>
    <div class="container">
        <%- include('../partials/sidebar', { currentPage: currentPage }) %>
        <main class="main-content">
    <div class="page-header">
        <h1>Settings</h1>
        <p>Manage your account settings</p>
    </div>

    <div class="card">
        <h2>Account Information</h2>
        <table>
            <tr>
                <th>Username</th>
                <td><%= typeof adminUser !== 'undefined' ? adminUser.username : 'N/A' %></td>
            </tr>
            <tr>
                <th>Email</th>
                <td><%= typeof adminUser !== 'undefined' ? adminUser.email : 'N/A' %></td>
            </tr>
            <tr>
                <th>Last Login</th>
                <td><%= typeof adminUser !== 'undefined' && adminUser.lastLoginAt ? new Date(adminUser.lastLoginAt).toLocaleString() : 'Never' %></td>
            </tr>
            <tr>
                <th>Account Created</th>
                <td><%= typeof adminUser !== 'undefined' ? new Date(adminUser.createdAt).toLocaleString() : 'N/A' %></td>
            </tr>
        </table>
    </div>
        </main>
    </div>
    <script src="/js/admin.js"></script>
</body>
</html>
